# SmartV2X-CP Ultra — Environment Configuration
# ===============================================

# ── Database ───────────────────────────────────
# PostgreSQL is required for production. 
# Render provides this automatically.
DATABASE_URL=postgresql://user:pass@localhost:5432/v2x_db

# ── Security ───────────────────────────────────
# Secret key for JWT signing. Generate a strong random string.
JWT_SECRET=super-secret-v2x-key-at-industry-grade-3912
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# ── Redis (Optional) ───────────────────────────
# Used for caching and high-frequency rate limiting.
REDIS_URL=redis://localhost:6379/0

# ── Frontend (VITE) ────────────────────────────
# Base URL for the backend API.
# On localhost, usually blank or http://localhost:3000
VITE_API_URL=
